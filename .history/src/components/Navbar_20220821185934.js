import { Link } from "react-router-dom";

const Navbar = () => {
    return(
        <header>
            <nav>
                <Link to="/">oris</Link>
            </nav>
        </header>
    )
}

export default Navbar;