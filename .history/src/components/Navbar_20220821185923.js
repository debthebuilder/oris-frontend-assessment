import { Link } from "react-router-dom";

const Navbar = () => {
    return(
        <header>
            <nav>
                <Link to
            </nav>
        </header>
    )
}

export default Navbar;