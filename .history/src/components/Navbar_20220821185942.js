import { Link } from "react-router-dom";

const Navbar = () => {
    return(
        <header>
            <nav>
                <Link to="/">oris</Link>
                <ul
            </nav>
        </header>
    )
}

export default Navbar;