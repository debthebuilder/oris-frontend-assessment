import { Link } from "react-router-dom";

const Navbar = () => {
    return(
        <header>
            <nav>
                <Link t
            </nav>
        </header>
    )
}

export default Navbar;